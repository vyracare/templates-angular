name: PUBLISH

on:
  push:
    branches:
      - main

jobs:
  publish:
    name: Skip AWS deploy for template repository
    runs-on: ubuntu-latest
    steps:
      - name: Skip deploy
        run: echo "Template repository does not publish to AWS on main."
